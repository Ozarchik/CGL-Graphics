#include <cgl/mesh/common/meshcontroller.h>
#include <cgl/mesh/common/mesh.h>

// CGL::MeshController::MeshController(CGL::Mesh& mesh)
//     m_mesh(mesh)
// {

// }

void CGL::MeshController::setRenderMode(CGL::Shader& shader, CGL::ColorRenderMode mode)
{
    if (mode == CGL::ColorRenderMode::COLOR) {
    } else {

    }
}